#pragma once

#include <fstream>
#include <vector>
#include <map>
#include <algorithm>

using namespace std;

namespace TexIO
{
	using byte = unsigned char;
	using uint = uint32_t;
	using ushort = uint16_t;
}